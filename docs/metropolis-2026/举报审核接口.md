# 举报审核接口与验证

更新：2026-09-16。后端、管理端及移动端举报确认/状态列表已实现；通知投递、证据保留策略、AI 消费端过滤及生产发布尚未完成。

## 用户接口

- `POST /review-reports/threads/:threadId/messages/:messageId`：需要用户 JWT、当前线程访问权限，不能举报自己。请求包括 `expectedRevision`、`reason`（1–1000 字符且非空白）、`acceptDisclosure: true` 和 `disclosureVersion: "2026-09-16"`。
- 同一人举报同一文字同一版本幂等；旧版本返回 409。举报本身不隐藏文字，不自动认定违规。
- `GET /review-reports?page=1&pageSize=20`：只返回自己的举报状态、原因和处理说明，不返回审核证据。

移动端提交前必须明确说明并要求确认：会向管理员披露该账单主人与该评价者之间的整条私密线程及历史文字版本。不能只写“提交举报”，也不能默认勾选同意。手机端 0.0.140 已接入此确认界面。

## 管理接口和界面

管理端新增“举报审核”入口，支持状态筛选、分页、目标文字标识、上下文、版本展开、处理说明和审核结论。窄屏改为纵向记录，避免操作按钮被横向表格挤出。

- `GET /admin-api/moderation`：分页查询，`status` 可为 `pending/upheld/dismissed`。
- `GET /admin-api/moderation/:id/evidence`：分页读取举报时的留档证据，每次读取写入管理员审计。证据不包含其他评价者线程或账单备注、原始通知等业务字段。
- `POST /admin-api/moderation/:id/decision`：`status` 为 `upheld/dismissed`，`reason` 为必填处理说明。普通用户 JWT 不可访问。最终处理只允许一次；相同结论和说明的重试幂等，不同结论返回 409。

举报时在权限锁和事务内冻结同一线程的文字与历史版本。之后修改或删除原文不覆盖证据。原账户或线程删除时外键置空，证据保留用于审核；具体保留期限、删除策略和用户披露需在生产上线前补齐，当前没有自动清理任务。

确认违规后隐藏目标文字当前版本和全部历史版本：普通详情返回空正文，历史接口拒绝读取，作者不能自行恢复。票数不变，其他回复可继续。管理员仍可读取专用证据接口。驳回举报不隐藏文字。

处理结果写入 `SocialInboxEvent`：举报人收到结果，成立时作者也收到；重试不会重复生成事件。这里只完成持久化，未接入系统推送或用户端未读列表。后续 AI/导出必须过滤 `hidden`，不能绕过普通阅读约束。

## 迁移与验证

迁移：`backend/prisma/migrations/202609160006_review_moderation/migration.sql`，新增举报、证据和审核记录三张表，不修改现有账单数据。生产执行前需备份并按前序迁移顺序应用；不能以 `db push` 代替生产迁移。代码回滚时可暂留新增表，禁止删除证据作为常规回滚手段。

本地已完成：

- Nest 编译、34 项单元测试、管理端 JavaScript 语法检查。
- 独立 PostgreSQL 16 数据库先创建上一版结构，再执行新增 SQL，运行 `backend/test/moderation.integration.cjs`。覆盖披露确认、跨人隔离、管理员鉴权、冻结版本、分页/审计、隐藏后的历史权限、并发判定、幂等事件、驳回不隐藏及账号删除后证据保留。
- Playwright 操作实际 Nest API + 独立测试数据库：登录、查看证据、提交成立结论、查看处理结果；桌面及 390px 窄屏截图已检查，脚本标签按文本显示，浏览器无报错。截图位于被忽略的 `output/playwright/moderation-*.png`，页面标注“本地测试 · 合成数据 · 未连接生产”。

生产仍因 SSH 身份认证被拒而待发布。R10 尚有证据保留策略、其他写入限流复核、系统通知及生产验证未闭合，不标记整体完成。应用内处理通知和 AI 报告失效已由后续增量接入。

## 举报提交限流

每账号滚动一小时最多五次新举报、滚动二十四小时最多二十次；已处理举报也计数，管理员结案不释放名额。相同内容版本的幂等重试先返回原举报，不重复占额或冻结证据。超过限额返回 HTTP 429 和中文提示；查看原有举报、处理结果和管理员审核不受此提交限额影响。

权限与版本检查先执行；计数及创建在持有举报人用户行锁的同一事务中执行，因此跨线程、多进程并发也不能突破限额。拒绝提交不会留下举报行或审核证据。此限制针对存储写入放大，不替代网关请求频率限制或全站内容治理策略。

迁移 `202609160014_report_rate_index` 添加 `(reporter_id, created_at)` 索引，不改写或删除现有数据。独立 PostgreSQL 16 先加载上一版 schema，再应用迁移并检查 Prisma diff 无差异；实际 HTTP 回归验证小时/每日剩一个名额时的跨线程竞争、幂等重试、被拒绝请求无证据写入及过期窗口恢复。Nest 编译通过。测试使用本机 15450 端口合成数据，容器测试后停止并自动移除。

## 手机端 0.0.140 / Android 161

- 私密对话中对方的未隐藏文字提供“举报此内容”；自己的文字没有该入口。
- 新增 `GET /review-reports/threads/:threadId/messages/:messageId`，在现有线程权限下重新读取目标版本和披露版本，跨人、自举报、已隐藏目标均拒绝。
- 举报页不传递路由正文，使用服务端目标预览；原因必填、同意默认未勾选。披露包含整条双向线程、撤回内容和全部历史版本，以及审核证据不随原文/账单/账号删除自动删除的事实。披露版本不匹配时要求升级。
- 提交使用预览时的版本；期间内容变化返回冲突，重新加载后必须重新填写并确认。重复同版本提交仍由后端幂等约束处理。
- 社群与隐私 → 我的举报，分页读取本人原因、状态和处理说明；不返回后台审核证据。

验证：后端编译、34 项单元测试和独立 PostgreSQL 举报审核集成测试通过；新增预览断言覆盖鉴权、作者、其他评价者隔离和隐藏后拒绝。移动端完整 TypeScript 通过；真实 RN 页面在 390×844 本地浏览器宿主中走通未确认禁用 → 提交 → 查看待审 → 合成管理员 API 判定 → 刷新结果 → 原文隐藏且无历史入口。截图在忽略目录 output/playwright/mobile-report-*.png。favicon 404 与 RN Web 弃用警告为预览宿主问题；返回已隐藏目标举报页时预览接口按设计返回 400。浏览器验证不替代真机回归，原有 App.test 原生 mock 缺失仍未解决。
Release 已构建并核验 com.zhizhang / 0.0.140 / 161，SHA-256：CB1397EE3E274EB418C89085D034E01C2D8D142BFF7666AACF782D4458A617AF。临时数据库、预览进程和浏览器均已停止。
