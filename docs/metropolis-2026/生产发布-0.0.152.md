# 0.0.152 生产发布记录

2026-09-17（北京时间）。后端 `de324db`，移动端 `3b04458`；已推送 `codex/track2-production`。APK 版本 `0.0.152`，Android versionCode `173`。

## 本次变化

按[自动记账与可选社群](自动记账与可选社群.md)纠正产品边界：取消人工核对前置条件，首页、统计、预算自动计入旧账单与新账单；底部社群入口随设置开关显示，启用后日常记账自动参榜。好友评账与消费复盘集中在社群。关闭社群不修改普通账本。

同时改善更新下载进度、失败重试、APK 缓存校验及安装权限提示。最终交互验收发现并修复开关成功后当前设置页未刷新的问题。

## 验证

- 后端 62 项单元测试，12 组数据库升级集成测试，四适配器 36 次合成 HTTP 请求；移动端 32 项测试及 TypeScript 检查通过。
- Android Release 构建成功；签名证书 SHA256 与此前已发布 APK 一致。RN Web 已查看首页、社群、五/六入口和启停交互；没有连接 Android 真机，因此不声称完成真机验收。
- 生产后端健康检查通过。临时账号通过实际 HTTP 创建未分类账单，自动汇总为 `-12.3400`、无待确认数量，启用社群自动上榜；关闭后账本不变，社群接口拒绝访问。临时账号及级联数据已删除，前后仍为 8 用户、154 账单、0 社群偏好、0 排名结果。
- 公网最新版本为 `0.0.152`；`0.0.151` 检测到更新，`0.0.152` 无更新。版本接口排序值 `152` 与 Android 原生 `173` 按既有设计分别使用。
- APK 72,416,691 字节，公网 HEAD 200，大小一致；服务器经本机 HTTPS 路由完整下载 SHA256 与本地一致。
- SHA256：`99c249a7138d9a9689a0285b5e086b6ae6553d75bc033031e75adb5637d08e7e`。
- 此前通知样本生产合成验收 58 项检查通过，1002 条样本验证分页导出与原文保真；两个临时用户已清理。该项不是手机真实通知捕获验收。

[下载 0.0.152](https://zhizhang.kagurayami.top/downloads/app-v0.0.152.apk)

## 部署位置、启停与清理

- 后端源码 `/opt/zhizhang/releases/track2-de324db/backend`，镜像 `zhizhang-backend:track2-de324db`，容器 `zhizhang-backend`。由已提交源码构建，不含其他未提交工作。
- APK 在 `/opt/zhizhang/data/downloads/app-v0.0.152.apk`，同目录 `app-latest.apk` 及原有兼容下载路径均同步。
- 编排入口 `/opt/zhizhang/docker-compose.yml`。在该目录执行 `docker compose stop backend` 停止、`docker compose up -d --no-deps --no-build backend` 启动、`docker compose logs --tail 100 backend` 看日志。本次无数据库结构迁移。
- 备份 `/opt/zhizhang/backups/track2-de324db/`，保留发布前数据库转储和 Compose；上一版镜像保留用于应用回滚。未改数据库卷或上传文件。
- 发布源码、APK、构建日志与备份有意保留。上传时容器 `/tmp` 的临时 APK 已删除；本地浏览器测试服务和隔离数据库停止并移除。`C:/b/update152-copy-backup/` 为本次修正构建目录复制问题的回滚备份，暂留本机，不提交。

其他 Track 02 工作仍以生产交付进度为准，本次发布不代表真实支付或全部原生链路已验收。
