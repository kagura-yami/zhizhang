# 0.0.164 卡片拖动方向锁定

Android versionCode 185，APK 40,296,899 字节（约 40.3 MB）。

## 调整与验证

- 首页预算与目标卡片按首次有效移动判定方向。横拖后忽略纵向偏移，并暂时关闭父页面滚动；竖滑交给页面，不触发卡片切换。
- 保留旋转、280ms 飞出与弹簧回弹。松手、取消、卡片数量变化和卸载均恢复滚动；动画中避免重复切换。单卡不拦截页面手势。
- TypeScript、移动端 40 项 Jest、既有 Android 49 项单元测试与 Release 构建通过。
- 实际 Dashboard 的 RN Web 浏览器触摸验证：横拖夹杂纵向偏移不滚页，竖滑正常滚页且不换卡，取消手势后恢复，下一张按钮正常。截图已目视检查。
- 未连接 Android 真机，原生实际手感仍待用户更新验证。

## 发布与运行

- 下载：https://zhizhang.kagurayami.top/downloads/app-v0.0.164.apk。
- SHA-256：daf23128c27ef7ed89da72e4e2fd752c4deecc7cd751c354818a614f4df48cde。签名与旧版一致。
- 发布到 /opt/zhizhang/data/downloads/，归档 /opt/zhizhang/releases/mobile-0.0.164/；同步稳定 APK、旧下载路径及 /app-version。HTTPS 文件完整哈希与本地一致。
- 后端保持 zhizhang-backend:device-0.0.163，本次没有数据库迁移或服务重启。
- 启停入口 /opt/zhizhang/docker-compose.yml；在 /opt/zhizhang 执行 docker compose stop backend / docker compose up -d --no-deps --no-build backend。
- 容器临时 APK 已删除；浏览器、预览服务与 SSH 交付前关闭。output/ 验证产物和生产版本归档保留，无新增常驻服务。
