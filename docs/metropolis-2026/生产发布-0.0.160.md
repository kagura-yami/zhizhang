# 0.0.160 首页卡片层次优化

Android versionCode 181，安装包 40,313,119 字节（约 40.3 MB）。

首页收支摘要采用淡金色主指标区和独立浅红/浅绿收支分区，钱包图标呼应账本，币种符号与金额区分字号。移除重复的“今日支出”等说明，显示对应日期或月份；结余口径说明仍保留。各个性化主指标/辅助指标继续使用原有数据与跳转。

预算与目标增加浅色图标底，进度条收细；不改卡堆手势、切换和回弹算法，也不改交易列表入场动画。

## 验证

- TypeScript、35 项 Jest、Release 构建及 49 项 Android 单元测试通过，签名与旧版一致。
- RN Web 实际组件/合成数据检查零金额、320/390 宽度和深色模式；触摸拖动与按钮切换通过。浏览器动画使用 JS 回退，真机流畅度未验收。
- 比对确认 PanResponder 和交易列表动画代码保持原样。截图保存在被忽略的 output/ui160-*.png。

## 发布与运行

- APK：https://zhizhang.kagurayami.top/downloads/app-v0.0.160.apk。
- SHA-256：bc107f739835b4e93eb3e8537ab84372890694dd8ee3a4b7db1b3d15a4a52dd4。
- 生产目录 /opt/zhizhang/data/downloads/，归档 /opt/zhizhang/releases/mobile-0.0.160/；同步稳定下载、旧 APK 路径和 /app-version 记录。
- 后端继续使用 zhizhang-backend:friends-0.0.157，无数据库变更。运行入口 /opt/zhizhang/docker-compose.yml，在该目录使用 docker compose stop backend / docker compose up -d --no-deps --no-build backend 启停。
- 临时浏览器与预览服务交付时关闭；output/ 验证记录及生产回滚归档保留，无额外常驻服务。
