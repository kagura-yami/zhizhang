# 0.0.151 生产发布记录

发布时间：2026-09-17（北京时间）。后端代码：`f789ee4`，分支 `codex/track2-production`。移动端版本 `0.0.151`，Android versionCode `172`。

## 发布结果与验证

- 服务器 `124.221.153.66` 已恢复登录，本批后端和 APK 已上线。凭据未写入项目、脚本或 Git。
- 从生产备份恢复隔离数据库，执行 001–016 全部增量迁移后，Prisma schema diff 无差异；保留 8 个用户、154 条账单。
- 暂停后端写入，制作最终备份，执行 `prisma migrate deploy`，再启动新版。生产现有两条历史迁移加本批共 18 条已完成，用户与账单数量不变。
- 后端真实 HTTP 健康检查通过；管理员登录、总览、通知样本列表及导出返回成功；匿名后台请求返回 401。公网管理页返回 200。
- 公网最新版本返回 `0.0.151`；旧版 `0.0.135` 检测到更新，新版 `0.0.151` 无更新。接口 `versionCode=151` 是现有服务按语义版本生成的排序值，与 APK 原生 `172` 不同。
- APK 大小 72,427,955 字节。服务器本地 HTTPS 下载链路完整 SHA256 与构建产物一致；公网 HEAD 返回 200 且大小一致。服务器绕公网完整下载校验超时，未将其计为通过。
- SHA256：`86974c183d71412c4c4051a2e3b357412851bc1edf417248cdfadbbb2dbfd837`。

下载：[0.0.151 APK](https://zhizhang.kagurayami.top/downloads/app-v0.0.151.apk)。

## 磁盘与 Docker 入口

- 本批源码：`/opt/zhizhang/releases/track2-f789ee4/backend`；由已提交的 Git archive 构建，未包含本地其他未提交改动。
- 镜像：`zhizhang-backend:track2-f789ee4`；容器：`zhizhang-backend`。
- 当前编排入口：`/opt/zhizhang/docker-compose.yml`。保留原生产环境文件、网络、数据库及上传/下载目录挂载。
- 版本 APK 与稳定下载文件位于 `/opt/zhizhang/data/downloads/`，兼容 APK 路径也已同步。
- 在 `/opt/zhizhang` 执行 `docker compose stop backend` 停止；执行 `docker compose up -d --no-deps --no-build backend` 启动；`docker compose logs --tail 100 backend` 查看日志。常规启停不会执行迁移。

后端启动命令已改为 `node dist/main.js`，移除 `db push --accept-data-loss`。健康检查改为实际请求 `/app-version/latest`。旧 `deploy/deploy.sh` 从裸仓库 main 导出源码且包含镜像清理流程，未经重新审查不要用于本批后端发布。`activate.sh` 是本次一次性发布记录，不是日常启停入口，也不要重跑覆盖备份。

## 备份与清理

- 保留 `/opt/zhizhang/backups/track2-f789ee4/`：预检备份、停写后最终备份、原 Compose 和原发布脚本。数据库备份权限为 600，仅在服务器保留。
- 保留回滚镜像 `zhizhang-backend:rollback-pre-track2-f789ee4`。数据库已升级，不能通过启动旧 Compose 的结构同步回滚。若必须恢复旧库，先停写并另存当时的新数据，再在明确处理新增写入后恢复最终备份。
- 临时数据库 `zhizhang_track2_preflight` 已删除；未删除任何生产卷、上传内容或历史 APK。本批源码、构建日志、APK 和备份是有意保留的发布记录。

## 验收边界

发布已解除此前 SSH 阻塞，但不代表全部 Track 02 需求完成。系统推送供应商、支付模式及支付闭环、举报证据保留策略、真实模型与 Android 真机端到端验收仍待完成。通知原文云端接口已可用，真实手机新通知上传与后台导出闭环仍需设备验证。
