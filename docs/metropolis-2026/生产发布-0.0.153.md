# 0.0.153 生产发布

2026-09-17。移动端提交 `1b576d5`，Android versionCode 174；后端继续使用 de324db，本批没有服务端或数据库变更。

普通设置仅保留“启用社群”。排行榜、我的反馈位于社群分页；消息和隐私位于社群顶部；评账申请、我的举报和黑名单位于社群“更多”。普通设置不再展示这些入口、用户 ID 或社群隐私选项。关闭后再次启用的同意勾选会重置。

验证：TypeScript、12 组 32 项移动端测试、Release 构建通过。390×844 RN Web 合成宿主检查普通设置、更多页及社群隐私跳转；这是布局和导航验证，不是真机端到端。签名与历史 APK 一致。公网最新版本 0.0.153，0.0.152 检测到更新，新版无更新；下载 HEAD 200，大小 72,416,863 字节。本机、服务器文件、稳定下载文件和服务器本机 HTTPS 完整下载的 SHA256 一致：`a95da257f6d0f88632daa68518c2408d69b28525ac9cbfc6d4e27f71269daa50`。

发布位置：`/opt/zhizhang/data/downloads/app-v0.0.153.apk`，`app-latest.apk` 与旧兼容下载路径已同步。归档 `/opt/zhizhang/releases/mobile-0.0.153/` 有意保留。服务启停仍在 `/opt/zhizhang` 使用 `docker compose stop backend` / `docker compose up -d --no-deps --no-build backend`；本次无需重启。容器上传临时 APK 已删除，本地预览进程已停止，截图留在忽略的 output 目录。
