# 知账多 Agent 协作记录

## 宣传网站

- 目录：`website/`
- 状态：本地独立宣传网站目录，整目录已加入根目录 `.gitignore`，不提交 GitHub。
- 作用：承载 `note.kagurayami.top` 的产品宣传页、功能与使用流程介绍、真实 App 素材展示、Android APK 下载、在线更新日志和联系入口。
- 资源来源：复用 `mobile/public/icons/` 品牌 Logo 与 `docs/screenshots/` App 截图/GIF；网站目录内保留部署所需副本。
- 版本数据：前端优先读取 `/app-version?platform=android` 和最新版本下载地址；接口不可用时使用 `0.0.66` 兜底信息。
- 联系方式：邮箱 `kagura_yami@Outlook.com`；联系页提供神楽知产品客服微信二维码与知账交流群二维码。
- 部署约定：网站静态文件需由公网入口的 Web Server 托管；`/downloads/*` 继续映射后端 `backend/public/downloads/`，`/app-version*` 反向代理到后端 API。不要把网站文件复制进 Git 跟踪范围。
- 验收：已在本地桌面与移动宽度检查首屏、真实素材、下载区、在线更新日志时间线、联系页两幕和下载链接；线上接口当前优先提供最新版本数据，接口不可用时使用内置兜底。
- 视觉迭代：2026-08-29 完成第二轮视觉减法，降低边框/方块/硬阴影密度，使用柔和渐变、圆润容器、品牌插画叠层光晕和滚动深度动画强化首屏冲击；移动端同步适配。
- 页面结构：2026-08-29 将宣传站拆为 `/` 首页、`/experience` 使用体验、`/download` 下载、`/changelog` 更新日志、`/contact` 联系（对应静态文件仍保留 `.html` 文件名）；已移除无独立价值的 `features.html` 能力页，相关入口统一指向使用体验。顶部导航使用无扩展名页面级链接，旧 `.html` 地址由后端 308 兼容重定向；桌面端滚轮一次进入下一视觉章节，移动端使用原生触控 + CSS scroll snap。
- 部署状态：2026-08-29 已将网站同步到本机 Docker 映射目录 `backend/public/site/` 并重建 `zhizhang-backend`；公网 `https://note.kagurayami.top/` 已返回首页，`/app-version`、`/downloads/app-latest.apk`、`/api-docs` 均保持可用。完整链路和未来子域名迁移步骤见 `docs/宣传网站部署与服务链路.md`。
- 动效技术：2026-08-29 参考 Moonshot 首页的 UnicornStudio 氛围层与叙事滚动方向，在静态多页约束下接入 Canvas 粒子氛围、GSAP + ScrollTrigger 增强入场、CSS View Transition 页面切换，并保留 IntersectionObserver 降级；移动端关闭高耗能粒子层。
- 交互回归：2026-08-29 修正样式末尾的导航定位覆盖，确保桌面导航悬浮于首屏；验证桌面一次滚轮可准确吸附到下一章节，移动端 390×844 菜单可展开且无可见横向滚动，页面与 `/app-version`、APK 下载链路保持可用。
- 品牌校对：2026-08-29 统一宣传站、站点 README 与页面元信息中的产品名为“知账”，并将所有页面左上角字标改为完整比例的 `assets/logo.png` 猫咪 Logo，避免首页与内页品牌标记不一致。
- 页面结构复核：2026-08-29 首页第二屏加入真实 `homeCard.gif` 动态预览，体验页首屏加入 App 界面和三步提示；顶部导航改为固定胶囊浮层，移动端单列重排。修复 GSAP 残留 opacity/transform 导致的流程内容空白，线上已重新同步并重启 Docker。
- Image2 链路复核：2026-08-29 使用 NewAPI `/v1/images/generations` 成功调用 `gpt-image-2`，返回 1 条 `b64_json` 图片结果；密钥仅在本机进程中读取，未写入项目。
- Motion 动效复核：2026-08-29 接入 Motion for JavaScript（`animate`、`inView`、`scroll`），线上移动端实测 `data-motion=ready`，顶部浮层无横向溢出，控制台无错误。
- 交互与比例复核：2026-08-29 将首屏插画缩至约 96% 宽度以避免裁切，第二幕手机 pointermove 改为按帧节流 + 可停止的 Motion spring；点击金币、长按笔形光标与金色轨迹、背景波纹均已挂载。第三幕补充月度回顾信息并压缩大字号，页脚收回至轻量 96px 高度；桌面与移动端重新部署到 `backend/public/site/`。
- 移动端回归修复：2026-08-29 修正折叠菜单定位覆盖，展开为整宽不透明抽屉；体验页支付/AI/图表长图改为完整比例显示；联系页卡片改为邮箱整行、QQ/微信并列，隐私与 FAQ 间距收紧；浅色页脚提高对比度。顶部下载入口改为进入 `/download`，首屏艺术图进一步收束到桌面 640px、移动 520px 上限。线上已重新同步并重启 Docker。
- 第二轮结构收束：2026-08-29 下载页信任证明取消独立吸附章节；体验页截图改为完整纵向展示；联系页改为分隔线联系方式列表；Orbit 降低装饰强度；移动端菜单不透明整宽、首屏插画宽度进一步收紧。网站已重新同步到 `backend/public/site/` 并重启 Docker。
- 吸附与叙事重构：2026-08-29 删除能力页及导航入口，入口统一指向使用体验；滚轮吸附改为 RAF quintic ease-in-out 慢→快→慢过渡，动画期间拦截后续滚轮并暂停原生 snap 与 CSS smooth scroll，长内容章节在边缘前保持自然滚动，降低突然跳幕；联系页隐私/FAQ 章节补足高度；手机姿态改为全页面指针影响的速度/阻尼弹簧；首页最后一幕 Orbit 改为使用知账 Logo 的账本信号视觉，移动端末幕改为单列排版。网站已重新部署。
- 联系页收束：2026-08-29 移除联系页下方无关的隐私与 FAQ 宽章节，只保留联系说明与联系方式列表两幕，页脚紧接其后并重新校验移动端总高度。
- 联系方式交互：2026-08-29 QQ 改为 `tencent://AddContact` 添加好友协议；微信号点击复制到剪贴板，并提供嵌入式预览下的降级复制和状态反馈。
- 视觉增强：2026-08-29 在现有 Motion/GSAP/Canvas 动效基础上补充统一环境光、细网格、章节编号与分隔线，导航激活点、按钮扫光、流程截图、下载环、联系方式和更新条目增加克制的 hover/呼吸反馈；已同步 `backend/public/site/` 并重启 Docker，公网首页返回 200。
- 联系页二维码：2026-08-30 将用户提供的微信群二维码保存为 `website/assets/wechat-group-qr.png`，并加入联系页联系方式列表下方；保持两幕结构，移动端二维码按比例缩放，已同步部署目录并验证无横向溢出。
- 联系页显示修复：2026-08-30 为联系页增加独立布局标记，收束大屏首屏高度并覆盖 Logo 原图放大规则，移除异常露出的装饰图；二维码资源与联系页均已重新部署并验证返回 200。
- 图像生成：尝试通过用户提供的 NewAPI 调用 `gpt-image-2` 时收到 `401 Invalid token`，未将密钥写入项目，也未覆盖现有品牌素材。
- 边缘收束：2026-08-30 移除定宽章节容器上的背景渐变，改用 `main` 全视口渐隐氛围层；联系/更新日志/页脚分隔线降低对比度并渐变收尾，导航材质向下淡出。已同步部署目录并在桌面宽屏与 390px 移动视口复核。
- 联系入口更新：2026-08-30 按用户新素材移除 QQ 与个人微信号，保留邮箱；联系页改为客服微信二维码与知账微信群二维码双入口，桌面并列、移动堆叠。新增 `website/assets/wechat-service-qr.png`，群二维码继续使用 `wechat-group-qr.png`，已重新部署并验证两个资源返回 200。
