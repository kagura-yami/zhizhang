<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ 系统通知权限（自动记账成功提醒） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 前台服务权限（用于 Headless JS） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- 唤醒锁权限（用于后台任务） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 请求忽略电池优化，确保支付通知监听在后台持续运行 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 接收开机广播权限（可选，用于开机自启） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 查询所有应用权限（用于应用选择器） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- 安装 APK 权限 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 麦克风权限（用于语音录制） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- MagicOS 等系统会额外限制应用可见性，显式声明常用支付与短信应用 -->
    <queries>
        <package android:name="com.tencent.mm" />
        <package android:name="com.eg.android.AlipayGphone" />
        <package android:name="com.xunmeng.pinduoduo" />
        <package android:name="com.icbc" />
        <package android:name="com.hihonor.mms" />
        <package android:name="com.android.mms" />
        <package android:name="com.google.android.apps.messaging" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">

      <!-- 禁用 Sentry 自动初始化，使用手动初始化 -->
      <meta-data android:name="io.sentry.auto-init" android:value="false" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="zhizhang" android:host="invoice-mailbox" android:pathPrefix="/oauth/callback" />
        </intent-filter>
      </activity>
      
      <!-- 支付通知监听服务 -->
      <service
          android:name=".notification.PaymentNotificationService"
          android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
          android:exported="false">
          <intent-filter>
              <action android:name="android.service.notification.NotificationListenerService" />
          </intent-filter>
          <meta-data
              android:name="android.service.notification.default_filter_types"
              android:value="conversations|alerting|silent|ongoing" />
      </service>
      
      <!-- Headless JS 服务（用于后台处理支付通知） -->
      <service
          android:name=".notification.PaymentNotificationHeadlessService"
          android:exported="false"
          android:foregroundServiceType="specialUse">
          <property
              android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
              android:value="payment_notification_processing" />
      </service>

      <service
          android:name=".shortcut.EdgeGestureService"
          android:exported="false"
          android:foregroundServiceType="specialUse">
          <property
              android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
              android:value="voice_edge_gesture" />
      </service>

      <!-- 独立语音悬浮层：录音和记账期间不启动主界面 -->
      <service
          android:name=".shortcut.VoiceCaptureService"
          android:exported="false"
          android:foregroundServiceType="microphone" />

      <receiver
          android:name=".shortcut.VoiceInputReceiver"
          android:exported="false" />

      <!-- 通知栏快捷设置中的语音输入入口 -->
      <service
          android:name=".shortcut.VoiceInputTileService"
          android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
          android:exported="true"
          android:label="知账语音">
          <intent-filter>
              <action android:name="android.service.quicksettings.action.QS_TILE" />
          </intent-filter>
      </service>
      
      <!-- 开机启动接收器（可选） -->
      <receiver
          android:name=".notification.BootCompletedReceiver"
          android:exported="false">
          <intent-filter>
              <action android:name="android.intent.action.BOOT_COMPLETED" />
          </intent-filter>
      </receiver>

      <!-- 桌面账务总览组件 -->
      <receiver
          android:name=".widget.FinanceOverviewWidget"
          android:exported="true">
          <intent-filter>
              <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
              <action android:name="com.zhizhang.widget.ACTION_CONFIG_CHANGED" />
              <action android:name="com.zhizhang.widget.ACTION_DATA_CHANGED" />
          </intent-filter>
          <meta-data
              android:name="android.appwidget.provider"
              android:resource="@xml/finance_overview_widget_info" />
      </receiver>

      <!-- FileProvider 用于安装 APK -->
      <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
              android:name="android.support.FILE_PROVIDER_PATHS"
              android:resource="@xml/file_paths" />
      </provider>
    </application>
</manifest>
